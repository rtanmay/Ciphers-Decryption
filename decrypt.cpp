#include <bits/stdc++.h>
using namespace std;
int main()
{
	cout<<"Enter the maximum size of the cipher text:";
	int n;
	cin>>n;
	char CT[n],c;
	s
	//check for caesar first
	c=caesar(CT);
	if(c=='y')
	{

	}

}